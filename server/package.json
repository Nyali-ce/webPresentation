{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "compiled/server.js",
  "scripts": {
    "build:server": "tsc",
    "build:client": "cd ../client && npm run build && cd ../server",
    "build": "npm run build:server && npm run build:client",
    "start": "npm run build && node .",
    "dev": "npm run build && nodemon -e ts ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "type": "module",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "typescript": "^5.0.4"
  }
}
